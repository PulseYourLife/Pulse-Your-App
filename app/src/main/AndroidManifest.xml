<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pulseyourlife">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.Main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Controller.Login"
            android:label="@string/login_name" />
        <activity android:name=".Controller.Register" android:label="@string/register_name"/>
        <activity android:name=".Controller.Profile" android:label="@string/profile_name"/>
        <activity android:name=".Controller.About" android:label="@string/about_name"/>
        <activity android:name=".Controller.Statistics" android:label="@string/statistics_name" android:noHistory="true"></activity>
    </application>

</manifest>